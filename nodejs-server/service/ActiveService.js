'use strict';


/**
 * Select a package service
 * When the user opts for select a package option to send data
 *
 * item Item_1 object containing the array of selected options
 * no response value expected for this operation
 **/
exports.cyo = function(item) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}


/**
 * Select a package service
 * When the user opts for select a package option to send data
 *
 * item Item object containing the array of selected options
 * no response value expected for this operation
 **/
exports.sap = function(item) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

